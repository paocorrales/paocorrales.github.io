comments:
  allowedFields: ["name", "email", "site", "content"]
  allowedOrigins: ["localhost", "paocorrales.github.io"]
  branch: "master"
  commitMessage: "add [comment]: by {fields.author} <Staticman>\n\n{fields.permalink}#comment-{@id}"
  filename: "{@id}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "timestamp"
  moderation: false
  name: "Pao Corrales"
  path: "data/comments/{options.postId}"
  requiredFields: ["name", "email"]
  transforms:
    email: md5
